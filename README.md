# JustAConter

Simple React Native app with a single screen counter.

Run (requires Node.js and Expo CLI):

```bash
npm install -g expo-cli
npm install
npm run start
```

Then open the project in Expo Go or run on a simulator/device.

Files:
- App.js — main app screen (Add, counter, Reset)
